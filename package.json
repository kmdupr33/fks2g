{
  "name": "fks2g",
  "version": "0.2.0",
  "description": "A CLI that estimates code change risk from git history, bug-fix commits, and GitHub issue similarity.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "fks2g": "./dist/bin/fks2g.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node ./dist/bin/fks2g.js",
    "check": "npm run build && npm test",
    "test": "node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@ai-sdk/openai": "^3.0.64",
    "ai": "^6.0.188",
    "commander": "^14.0.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
